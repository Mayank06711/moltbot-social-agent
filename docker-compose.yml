services:
  kyf-agent:
    build: .
    container_name: kyf-agent
    restart: unless-stopped
    env_file: .env
    volumes:
      - kyf-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  kyf-data:
